[package]
name = "private-swap-router-methods"
version = "0.1.0"
edition = "2021"

[build-dependencies]
risc0-build = "=3.0.5"

[dependencies]
risc0-zkvm = { version = "=3.0.5", features = ["std"] }
private_swap_router_core = { path = "../core" }

[package.metadata.risc0]
methods = ["guest"]
