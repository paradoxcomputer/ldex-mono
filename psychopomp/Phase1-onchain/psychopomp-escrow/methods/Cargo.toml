[package]
name = "psychopomp-escrow-methods"
version.workspace = true
edition.workspace = true
license.workspace = true

[build-dependencies]
risc0-build = "=3.0.5"

[dependencies]
risc0-zkvm = { version = "=3.0.5", features = ["std"] }

[package.metadata.risc0]
methods = ["guest"]
